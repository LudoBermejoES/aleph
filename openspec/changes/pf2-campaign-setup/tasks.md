## 1. Script infrastructure

- [ ] 1.1 Create `scripts/pf2-setup/` directory and `scripts/pf2-setup/lib/cli.js` — a helper that reads `~/.aleph/config.json`, builds `Authorization: Bearer <apiKey>` headers, and exports `alephFetch(path, method, body)` for direct HTTP calls to the configured server URL
- [ ] 1.2 Create `scripts/pf2-setup/lib/templates.js` — exports `ENTITY_TYPE_TEMPLATES` object with field definitions for all 14 PF2e entity types (see design.md), and `CHARACTER_TEMPLATES` array with PC and NPC/Creature template definitions
- [ ] 1.3 Create `scripts/pf2-setup/lib/currencies.js` — exports PF2e currency definitions: `[{ name: "Moneda de Platino", symbol: "pp", value: 1000 }, { name: "Moneda de Oro", symbol: "go", value: 100 }, { name: "Moneda de Plata", symbol: "pa", value: 10 }, { name: "Moneda de Cobre", symbol: "pc", value: 1 }]`
- [ ] 1.4 Create `scripts/pf2-setup/lib/format.js` — exports formatters for building markdown content from JSON data: `formatSpell(spell)`, `formatWeapon(weapon)`, `formatArmor(armor)`, `formatItem(item)`, `formatFeat(feat)`, `formatAction(action)`, `formatShield(shield)`. Each returns a markdown string with the appropriate stat block layout (action symbols, bold labels, horizontal rules).

## 2. Setup script — campaign and structure

- [ ] 2.1 Create `scripts/pf2-setup/setup.js` with a `--name <name>` CLI flag (using Node's `process.argv` or a minimal arg parser). Parse the flag, check `~/.aleph/config.json` exists and has `apiUrl` and `apiKey`, or exit with a clear error message in Spanish (e.g. "Error: no se encontró configuración. Ejecuta 'aleph login' primero.").
- [ ] 2.2 In `setup.js`, call `GET /api/campaigns` (via `alephFetch`), search for an existing campaign with the given name; if found, print `La campaña ya existe: <id>` and exit 0 to ensure idempotency.
- [ ] 2.3 In `setup.js`, call `POST /api/campaigns` with `{ name, theme: "high-fantasy" }` to create the campaign. Print `Campaña creada: <id>`.
- [ ] 2.4 In `setup.js`, create 14 custom entity types by calling `POST /api/campaigns/<id>/entity-types` for each, with Spanish display names: `pf2-conjuro` ("Conjuro"), `pf2-clase` ("Clase"), `pf2-ascendencia` ("Ascendencia"), `pf2-herencia` ("Herencia"), `pf2-trasfondo` ("Trasfondo"), `pf2-dote` ("Dote"), `pf2-accion` ("Acción"), `pf2-arma` ("Arma"), `pf2-armadura` ("Armadura"), `pf2-escudo` ("Escudo"), `pf2-objeto` ("Objeto"), `pf2-rasgo` ("Rasgo"), `pf2-condicion` ("Condición"), `pf2-arquetipo` ("Arquetipo"). Print progress in Spanish.
- [ ] 2.5 In `setup.js`, create entity templates for all 14 entity types using field definitions from `lib/templates.js`. For each type, call `POST /api/campaigns/<id>/templates` with `{ name: "<NombreEspañol> PF2e", entityTypeSlug: "<slug>", isDefault: true, fields: [...] }`. Template names must be fully in Spanish (e.g. "Conjuro PF2e", "Clase PF2e", "Ascendencia PF2e"). Print progress in Spanish.
- [ ] 2.6 In `setup.js`, create the "PJ PF2e" character template (Personaje Jugador): call `POST /api/campaigns/<id>/templates` with `{ name: "PJ PF2e", entityTypeSlug: "character", isDefault: false, fields: <PC_FIELDS from lib/templates.js> }`. Print template ID.
- [ ] 2.7 In `setup.js`, create the "PNJ/Criatura PF2e" character template: call `POST /api/campaigns/<id>/templates` with `{ name: "PNJ/Criatura PF2e", entityTypeSlug: "character", isDefault: true, fields: <NPC_FIELDS from lib/templates.js> }`. Print template ID. (PNJ/Criatura is default since most characters created are NPCs.)
- [ ] 2.8 In `setup.js`, create 4 PF2e currencies using `lib/currencies.js` definitions via `POST /api/campaigns/<id>/currencies`. Print each currency name and ID in Spanish.
- [ ] 2.9 In `setup.js`, print a completion summary in Spanish: ID de campaña, número de tipos de entidad, número de plantillas, número de monedas, and instructions to run `seed-entities.js`.

## 3. PC template — field definitions

All field `label` values and section names must be in Spanish. The `key` values stay as short English identifiers (used internally). All select option values must also be in Spanish.

- [ ] 3.1 In `lib/templates.js`, define `PC_CHARACTER_FIELDS` with a section divider label "Datos Principales" and fields: `level` (label "Nivel", number, required), `ancestry` (label "Ascendencia", text), `heritage` (label "Herencia", text), `background` (label "Trasfondo", text), `class` (label "Clase", text, required), `subclass` (label "Subclase / Patente", text)
- [ ] 3.2 Add section "Puntuaciones de Atributo" with 6 number fields: `str` (label "Fuerza"), `dex` (label "Destreza"), `con` (label "Constitucion"), `int` (label "Inteligencia"), `wis` (label "Sabiduria"), `cha` (label "Carisma") — all required
- [ ] 3.3 Add section "Defensas" with fields: `max_hp` (label "PG Maximos", number, required), `current_hp` (label "PG Actuales", number), `ac` (label "Clase de Armadura", number, required), `fortitude` (label "Fortaleza", text), `reflex` (label "Reflejos", text), `will` (label "Voluntad", text), `perception` (label "Percepcion", text)
- [ ] 3.4 Add section "Ataque" with fields: `class_dc` (label "CD de Clase", number), `spell_attack` (label "Ataque de Conjuro", number), `spell_dc` (label "CD de Conjuro", number)
- [ ] 3.5 Add section "Movimiento" with fields: `speed` (label "Velocidad (pies)", number, required), `fly_speed` (label "Velocidad de Vuelo", number), `swim_speed` (label "Velocidad de Natacion", number), `climb_speed` (label "Velocidad de Escalada", number), `burrow_speed` (label "Velocidad de Excavar", number)
- [ ] 3.6 Add section "Habilidades" with 17 text fields using Spanish labels: `acrobatics` ("Acrobacias"), `arcana` ("Arcanos"), `athletics` ("Atletismo"), `crafting` ("Artesania"), `deception` ("Engano"), `diplomacy` ("Diplomacia"), `intimidation` ("Intimidacion"), `lore` ("Saber"), `medicine` ("Medicina"), `nature` ("Naturaleza"), `occultism` ("Ocultismo"), `performance` ("Interpretacion"), `religion` ("Religion"), `society` ("Sociedad"), `stealth` ("Sigilo"), `survival` ("Supervivencia"), `thievery` ("Latrocinio")
- [ ] 3.7 Add section "Dotes y Caracteristicas" with 5 textarea fields: `ancestry_feats` (label "Dotes de Ascendencia"), `class_feats` (label "Dotes de Clase"), `skill_feats` (label "Dotes de Habilidad"), `general_feats` (label "Dotes Generales"), `class_features` (label "Caracteristicas de Clase")
- [ ] 3.8 Add section "Lanzamiento de Conjuros" with fields: `tradition` (label "Tradicion", select: Arcana/Divina/Ocultista/Primigenia/—), `casting_type` (label "Tipo de Lanzamiento", select: Preparados/Espontaneos/—), `cantrips` (label "Trucos", number), `spell_slots_1` through `spell_slots_10` (labels "Espacios Nivel 1" … "Espacios Nivel 10", number), `focus_pool` (label "Puntos de Foco", number)
- [ ] 3.9 Add section "Equipo" with fields: `armor_worn` (label "Armadura", text), `shield` (label "Escudo", text), `weapons` (label "Armas", textarea)
- [ ] 3.10 Add section "Recursos" with fields: `hero_points` (label "Puntos de Heroe", number), `focus_points` (label "Puntos de Foco Actuales", number), `special_resources` (label "Recursos Especiales", textarea)
- [ ] 3.11 Add section "Notas" with fields: `personality` (label "Personalidad", textarea), `appearance` (label "Apariencia", textarea), `backstory` (label "Trasfondo Narrativo", textarea), `allies` (label "Aliados", textarea), `enemies` (label "Enemigos", textarea), `notes` (label "Notas", textarea)

## 4. PNJ/Criatura template — field definitions

All field `label` values and section names must be in Spanish.

- [ ] 4.1 In `lib/templates.js`, define `NPC_CHARACTER_FIELDS` with section "Identidad": `creature_type` (label "Tipo de Criatura", text), `level` (label "Nivel", number, required), `alignment` (label "Alineamiento", text), `size` (label "Tamanio", select: Diminuto/Pequeno/Mediano/Grande/Enorme/Descomunal), `rarity` (label "Rareza", select: Comun/Poco comun/Raro/Unico), `traits` (label "Rasgos", text)
- [ ] 4.2 Add section "Caracteristicas" with fields: `perception` (label "Percepcion", text, e.g. "+8"), `senses` (label "Sentidos", textarea), `languages` (label "Idiomas", text), `skills` (label "Habilidades", textarea)
- [ ] 4.3 Add section "Modificadores de Atributo" with fields: `str` (label "Fuerza"), `dex` (label "Destreza"), `con` (label "Constitucion"), `int` (label "Inteligencia"), `wis` (label "Sabiduria"), `cha` (label "Carisma") — all number (these are modifiers, not scores)
- [ ] 4.4 Add section "Defensas" with fields: `ac` (label "Clase de Armadura", number, required), `max_hp` (label "Puntos de Golpe", number, required), `fortitude` (label "Fortaleza", text), `reflex` (label "Reflejos", text), `will` (label "Voluntad", text), `immunities` (label "Inmunidades", text), `resistances` (label "Resistencias", text), `weaknesses` (label "Debilidades", text), `special_defenses` (label "Defensas Especiales", textarea)
- [ ] 4.5 Add section "Ataque" with fields: `speed` (label "Velocidad", text, e.g. "25 pies, vuela 40 pies"), `melee_attacks` (label "Ataques Cuerpo a Cuerpo", textarea), `ranged_attacks` (label "Ataques a Distancia", textarea), `spells` (label "Conjuros", textarea), `special_abilities` (label "Habilidades Especiales", textarea)
- [ ] 4.6 Add section "Fuente" with fields: `source_book` (label "Manual", text), `page` (label "Pagina", text), `xp_award` (label "XP Otorgada", number), `treasure` (label "Tesoro", text)

## 5. Entity type template field definitions

All field `label` values and section names must be in Spanish. `key` values are short English identifiers. All `select` option values must be in Spanish.

- [ ] 5.1 In `lib/templates.js`, define `SPELL_FIELDS` with Spanish labels: `level` ("Nivel", number, required), `is_cantrip` ("Es Truco", checkbox), `traditions` ("Tradiciones", text, required), `actions` ("Acciones", text, e.g. "◆◆"), `range` ("Alcance", text), `targets` ("Objetivos", text), `area` ("Area", text), `duration` ("Duracion", text), `saving_throw` ("Salvacion", text), `traits` ("Rasgos", text), `description` ("Descripcion", textarea, required), `heightened` ("Potenciado", textarea)
- [ ] 5.2 Define `CLASS_FIELDS` with Spanish labels: `complexity` ("Complejidad", select: Sencilla/Media/Compleja), `hp_per_level` ("PG por Nivel", number, required), `key_ability` ("Atributo Clave", text, required), `perception_prof` ("Percepcion", select: No entrenado/Entrenado/Experto/Maestro/Legendario), `fortitude_prof` ("Fortaleza", select same options), `reflex_prof` ("Reflejos", select same options), `will_prof` ("Voluntad", select same options), `trained_skills` ("Habilidades con Entrenamiento", text), `extra_skills` ("Habilidades Adicionales", text), `class_features_summary` ("Resumen de Caracteristicas", textarea)
- [ ] 5.3 Define `ANCESTRY_FIELDS` with Spanish labels: `hp` ("Puntos de Golpe", number, required), `size` ("Tamanio", select: Pequeno/Mediano/Grande), `speed` ("Velocidad (pies)", number, required), `attribute_boosts` ("Mejoras de Atributo", text, required), `attribute_flaw` ("Defecto de Atributo", text), `languages` ("Idiomas", text, required), `traits` ("Rasgos", text, required), `special_abilities` ("Habilidades Especiales", textarea)
- [ ] 5.4 Define `HERITAGE_FIELDS` with Spanish labels: `ancestry` ("Ascendencia", text, required), `traits` ("Rasgos", text), `benefit` ("Beneficio", textarea, required)
- [ ] 5.5 Define `BACKGROUND_FIELDS` with Spanish labels: `attribute_boosts` ("Mejoras de Atributo", text, required), `skill_training` ("Entrenamiento en Habilidad", text, required), `skill_feat` ("Dote de Habilidad", text, required), `lore_skill` ("Habilidad de Saber", text), `special` ("Especial", textarea)
- [ ] 5.6 Define `FEAT_FIELDS` with Spanish labels: `level` ("Nivel", number, required), `category` ("Categoria", select: Clase/Ascendencia/Habilidad/General/Arquetipo), `class_or_ancestry` ("Clase o Ascendencia", text), `action_type` ("Tipo de Accion", text), `prerequisites` ("Prerequisitos", text), `traits` ("Rasgos", text, required), `benefit` ("Beneficio", textarea, required), `special` ("Especial", textarea)
- [ ] 5.7 Define `ACTION_FIELDS` with Spanish labels: `action_type` ("Tipo de Accion", select: ◆/◆◆/◆◆◆/↺/◇/Variable), `category` ("Categoria", select: Basica/Especialidad/Exploracion/Tiempo libre), `traits` ("Rasgos", text), `trigger` ("Desencadenante", text), `requirements` ("Requisitos", text), `critical_success` ("Exito Critico", textarea), `success` ("Exito", textarea), `failure` ("Fallo", textarea), `critical_failure` ("Fallo Critico", textarea)
- [ ] 5.8 Define `WEAPON_FIELDS` with Spanish labels: `price` ("Precio", text), `damage` ("Dano", text, required, e.g. "1d8 C"), `hands` ("Manos", select: 1/1+/2), `bulk` ("Bulto", text, required), `group` ("Grupo", text, required), `category` ("Categoria", text, required), `traits` ("Rasgos", text), `is_ranged` ("Es a Distancia", checkbox), `range` ("Alcance", text), `reload` ("Recarga", text)
- [ ] 5.9 Define `ARMOR_FIELDS` with Spanish labels: `price` ("Precio", text), `ac_bonus` ("Bono de CA", text, required), `dex_cap` ("Limite de DES", text, required), `check_penalty` ("Penalizacion a Pruebas", text), `speed_penalty` ("Penalizacion a Velocidad", text), `strength_req` ("Requisito de Fuerza", text), `bulk` ("Bulto", text, required), `group` ("Grupo", text, required), `category` ("Categoria", text, required), `weight_class` ("Tipo", select: Ligera/Intermedia/Pesada/Sin armadura), `traits` ("Rasgos", text)
- [ ] 5.10 Define `SHIELD_FIELDS` with Spanish labels: `price` ("Precio", text), `ac_bonus` ("Bono de CA", text, required), `hardness` ("Dureza", number, required), `hp` ("Puntos de Golpe", number, required), `bt` ("Umbral de Rotura", number, required), `bulk` ("Bulto", text, required), `traits` ("Rasgos", text)
- [ ] 5.11 Define `ITEM_FIELDS` with Spanish labels: `price` ("Precio", text), `bulk` ("Bulto", text), `hands` ("Manos", text), `traits` ("Rasgos", text), `item_type` ("Tipo de Objeto", text), `usage` ("Uso", text)
- [ ] 5.12 Define `TRAIT_FIELDS` with Spanish labels: `trait_type` ("Tipo de Rasgo", text, e.g. "dano", "accion", "conjuro", "criatura")
- [ ] 5.13 Define `CONDITION_FIELDS`: empty array — condition content lives entirely in the entity's markdown description
- [ ] 5.14 Define `ARCHETYPE_FIELDS` with Spanish labels: `dedication_feat` ("Dote de Dedicacion", text, required), `class_traits` ("Rasgos de Clase", text), `complexity` ("Complejidad", select: Sencilla/Media/Compleja)

## 6. Content formatters

All formatted markdown content must use Spanish labels (e.g. "**Nivel:**", "**Alcance:**", "**Tradiciones:**"). Only the code keys remain in English.

- [ ] 6.1 In `lib/format.js`, implement `formatSpell(spell)` — builds a Spanish markdown stat block: "**Nivel:** X | **Acciones:** ◆◆ | **Tradiciones:** Arcana, Primigenia", then "**Alcance:**", "**Objetivos:**", "**Area:**" (if present), "**Duracion:**", "**Salvacion:**" (if present), "**Rasgos:**", separator, description, then "**Potenciado (+X):**" entries. Action symbols: `◆` (1 accion), `◆◆` (2 acciones), `◆◆◆` (3 acciones), `↺` (reaccion), `◇` (accion libre).
- [ ] 6.2 Implement `formatWeapon(weapon)` — Spanish markdown with "**Dano:**", "**Manos:**", "**Bulto:**", "**Categoria:**", "**Grupo:**", "**Precio:**", traits with descriptions. For ranged weapons: "**Alcance:**" and "**Recarga:**".
- [ ] 6.3 Implement `formatArmor(armor)` — Spanish markdown with "**Bono de CA:**", "**Limite DES:**", "**Penalizacion a Pruebas:**", "**Penalizacion a Velocidad:**", "**Requisito de Fuerza:**", "**Bulto:**", "**Categoria:**", "**Grupo:**", "**Precio:**", traits with descriptions.
- [ ] 6.4 Implement `formatShield(shield)` — Spanish markdown with "**Bono de CA:**", "**Dureza:**", "**PG:**", "**Umbral de Rotura:**", "**Bulto:**", "**Precio:**", traits.
- [ ] 6.5 Implement `formatItem(item)` — Spanish markdown with "**Precio:**", "**Bulto:**", "**Manos:**", separator, description text.
- [ ] 6.6 Implement `formatFeat(feat)` — Spanish markdown with "**Nivel:**", "**Categoria:**", "**Clase/Ascendencia:**" (if present), "**Tipo de Accion:**" (if present), "**Prerequisitos:**" (if present), "**Rasgos:**", separator, benefit text, description text, "**Especial:**" (if present).
- [ ] 6.7 Implement `formatAction(action)` — Spanish markdown with action type symbol, "**Rasgos:**", "**Desencadenante:**" (if present), "**Requisitos:**" (if present), separator, description, and a results block using Spanish headers: "**Exito Critico:**", "**Exito:**", "**Fallo:**", "**Fallo Critico:**" (only if those fields are non-null in the JSON).
- [ ] 6.8 Implement `formatClass(classSlug, classData)` — Spanish markdown with "**Complejidad:**", "**PG por Nivel:**", "**Atributo Clave:**", proficiency table using Spanish proficiency levels (No entrenado/Entrenado/Experto/Maestro/Legendario), and a "**Caracteristicas de Clase**" placeholder section.
- [ ] 6.9 Implement `formatAncestry(ancestrySlug, ancestryData)` — Spanish markdown with "**PG:**", "**Tamanio:**", "**Velocidad:**", "**Mejoras de Atributo:**", "**Defecto de Atributo:**", "**Idiomas:**", "**Rasgos:**", separator, special abilities text.

## 7. Seed script

- [ ] 7.1 Create `scripts/pf2-setup/seed-entities.js` with flags: `--campaign <id>` (required), `--type <type>` (one of: spells/weapons/armors/shields/items/feats/actions/classes/ancestries/traits/all). Validate flags and print usage on error in Spanish.
- [ ] 7.2 Implement `seedSpells(campaignId, templates)` — reads `/Users/ludo/code/pf2/tools/spellCardCreator/data/spells.json`, finds the `pf2-spell` template ID from `GET /api/campaigns/<id>/templates`, then for each spell calls `POST /api/campaigns/<id>/entities` with `{ name, type: "pf2-spell", content: formatSpell(spell), templateId, fields: { level, is_cantrip, traditions, actions, range, targets, area, duration, traits, description, heightened } }`. Print progress every 10 spells.
- [ ] 7.3 Implement `seedWeapons(campaignId, templates)` — reads `tools/weaponsCardCreator/data/weapons.json`, creates one `pf2-weapon` entity per entry with formatted content and template fields. Print progress.
- [ ] 7.4 Implement `seedArmors(campaignId, templates)` — reads `tools/armorCardCreator/data/armors.json`, creates one `pf2-armor` entity per entry. Print progress.
- [ ] 7.5 Implement `seedShields(campaignId, templates)` — reads `tools/shieldCardCreator/data/shields.json` if it exists, creates `pf2-shield` entities. Print progress.
- [ ] 7.6 Implement `seedItems(campaignId, templates)` — reads `tools/itemCardCreator/data/items.json`, creates one `pf2-item` entity per entry. Print progress.
- [ ] 7.7 Implement `seedFeats(campaignId, templates)` — reads `tools/featCardCreator/data/feats.json`, creates one `pf2-feat` entity per entry. Print progress every 20 feats.
- [ ] 7.8 Implement `seedActions(campaignId, templates)` — reads `tools/actionsCardCreator/data/actions.json`, creates one `pf2-action` entity per entry with all result fields. Print progress.
- [ ] 7.9 Implement `seedClasses(campaignId, templates)` — hardcodes the 18 class names and slugs, reads their front matter from `docs/_clases/<slug>/index.md` using a YAML front-matter parser, creates one `pf2-class` entity per class with formatted content. Print progress.
- [ ] 7.10 Implement `seedAncestries(campaignId, templates)` — reads ancestry slugs from `docs/_ascendencias/` directory listing, reads front matter from `index.md` of each, creates one `pf2-ancestry` entity per ancestry. Print progress.
- [ ] 7.11 Implement `seedTraits(campaignId, templates)` — reads `tools/traitCardCreator/data/traits.json`, creates one `pf2-trait` entity per trait with `trait_type` field populated. Print progress.
- [ ] 7.12 In `seed-entities.js`, implement `--all` mode that calls all seed functions in sequence: spells, classes, ancestries, feats, weapons, armors, shields, items, actions, traits. Print a summary with count of entities created per type.

## 8. README and documentation

- [ ] 8.1 Create `scripts/pf2-setup/README.md` entirely in Spanish, with sections: "Requisitos" (Node.js, aleph CLI configurado via `aleph login`), "Inicio rapido" (quick start commands), "Que hace cada script", the full list of entity types (using Spanish names) and templates created, and an example of creating a PC character with the "PJ PF2e" template.
- [ ] 8.2 In `README.md`, include a section "Crear un Personaje Jugador" showing: how to create a character via `aleph character create`, then use the API to set template fields (Nivel, puntuaciones de atributo, PG, CA, habilidades), with a concrete example using curl or fetch with the API key from `~/.aleph/config.json`.
- [ ] 8.3 In `README.md`, document the seed data sources in Spanish and note that all content comes from the Spanish PF2e translation at `/Users/ludo/code/pf2`.
