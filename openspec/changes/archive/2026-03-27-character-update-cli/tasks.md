## 1. CLI

- [x] 1.1 Add `update <slug>` subcommand to `cli/src/commands/character.js` with options `--campaign`, `--name`, `--race`, `--class`, `--alignment`, `--status`, `--content`, `--stdin`; build body from only provided flags; error if no fields given; send via `put()`
- [x] 1.2 Update `docs/claude-skill.md` — add `character update` to command reference, bump version to 1.4
- [x] 1.3 Update `.claude/skills/aleph-cli/SKILL.md` — same, bump version to 1.5
