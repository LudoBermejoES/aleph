services:
  aleph:
    build: .
    ports:
      - "3033:3033"
    volumes:
      - aleph-data:/app/data
      - aleph-content:/app/content
      - aleph-logs:/app/logs
    environment:
      - NODE_ENV=production
    restart: unless-stopped

volumes:
  aleph-data:
  aleph-content:
  aleph-logs:
